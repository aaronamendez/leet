/*
1. function createCycle(head) - point the tail to the head *
2. function multiplyNext(head) - multiply the value in each node by the value in the next node *
3. function removeEveryOther(head) - skip every other node, head never changes *
4. function shiftDataLeft(head) - shift the data left without altering next pointers *
5. function findThirdQuarterNode(head) - find the node exactly 3/4ths length through the linked list. Hard-mode: solve it another way. **
*/
